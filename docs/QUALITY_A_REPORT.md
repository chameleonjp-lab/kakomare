# カコマレ PR-A 検証記録

実施日: 2026年9月8日（UTC）
作業ブランチ: `codex/quality-a-20260908`
基準main: `e6ca4229156819ccce8b47ef4273b2e0451d4d75`（PR #13）
計画: [QUALITY_IMPROVEMENT_PLAN.md](QUALITY_IMPROVEMENT_PLAN.md)

実装: Luna・Max
回帰検査: Luna・Max（実装と分担）
独立レビュー: Sol・High
検証記録とPRの集約: 主担当

## 対象

A01〜A05、A07、A06の終端破裂と重力崩壊を修正する工程です。最初の目的は、選べる操作と面の表示、照準と命中、危険の表示を一致させることです。PR-Bの画面刷新・補助効果の設計、PR-Cのバランス調整は含めません。

## 再現検査

修正前の基準mainで、監査と同じ実関数プローブを2026年9月8日に再実行しました。PhaserのSceneとPointだけを代替し、本番の判定関数とBattleSceneの攻撃処理を呼び出しています。ブラウザの表示や実機性能の検査ではありません。

| 条件 | 修正前の結果 | 修正後 |
|---|---|---|
| 手動179度、敵−179度 | 範囲内の敵1を選ばず、反対側の敵2を選んだ | 範囲内の敵1を選択 |
| 回転冠への240度と−120度の同方向攻撃 | 一方は防ぎ、他方は10被害を受けた | 両方とも防ぐ |
| 反射開始付近の光線と回転刃の斜め接触 | 入射面が線の始点・刃の内端に引っ張られた | 光線は進行方向の反対側、回転刃は回転接線の反対側 |
| コアから100の敵を外側へ吸引 | 座標20・内部距離180へ分離し、次更新で179.615へ飛んだ | 座標100・距離100のまま。次更新は99.615 |
| 連鎖終端のy=0とy=0.001 | 合計被害24と36.5で差が出た | どちらも36.5 |
| 重力崩壊の威力倍率1と2 | どちらも12.6被害だった | 12.6と25.2（2倍） |
| 予告中の投下体140と接触前の針走り1 | 最小演出で針走りを表示しなかった | active 141体をすべて表示し、危険表示あり |

倍率2は適用の有無を見るための検査値で、購入できる研究の上限を意味しません。予告中140体の構成は表示上限の境界検査で、自然な戦闘での発生率は未測定です。

A01の修正前根拠は、同じ基準mainの公開版における前回監査の実操作です。最後の除外に成功した後、新規候補の面ボタンが無効になりました。今回追加するブラウザ検査の結果とは分けて扱います。

## 検査環境

同一の `package-lock.json` に対して `npm ci --ignore-scripts --no-audit --no-fund` を実行し、162パッケージの導入が完了しました。通常の `npm ci` と同じ依存解決を行っていますが、検査用にライフサイクルスクリプト・監査・資金表示を省略しています。

開始時の既存ブラウザ検査1件は、Chromiumの実行ファイルが未導入のため起動前に失敗しました。これはゲームの判定失敗や修正前の再現検査の結果には数えません。`npx playwright install chromium webkit` も配信元への接続が繰り返しタイムアウトし、最後はHTTP 502で導入に失敗しました。Chromium・WebKitの必須検査はGitHub Actionsで実行します。

## ローカル検査の結果

- `npm run lint`：成功
- `npm run typecheck`：成功
- `npm test -- --testTimeout=30000`：20ファイル、135件すべて成功
- `npm run build`：成功。JavaScript 1,357.62 kB（gzip 377.41 kB）。Viteの500 kB超警告は残るため、性能合格とは扱わない
- `npm run verify:dist`：成功（CSS 1件、JavaScript 1件、OGP）
- `npm run verify:originality`：成功（テキスト107件、全ファイル110件）
- `git diff --check`：成功
- 実関数プローブ：修正後の表の値を再確認

ローカルのPlaywrightは実行ファイル未導入で起動できませんでした。`npx playwright install chromium webkit` も配信元のタイムアウトとHTTP 502で完了しなかったため、Chromium・WebKit検査はGitHub Actionsで確認します。

## 独立レビュー

Sol・Highが最終差分を読み取り専用で確認し、修正必須の指摘がないことを確認しました。レビュー途中で見つかった、seed 1の置換候補名の誤り、光線の幅内で入射面がずれる問題、周回刃の接線方向未反映、終端破裂テストの断言不足は同じブランチで修正し、全135件の検査を再実行しました。

## Draft PRの完了条件

- [ ] ChromiumとWebKitのGitHub Actions
- [x] Sol・Highによる独立レビュー：修正必須の指摘なし（ray/orbitの座標検査と終端破裂の断言を追加済み）
- [ ] Draft PRの最新コミットで上記の静的・回帰検査を再確認

## 実機に残す確認

iPhone 17 Pro・Safariの実機では未確認です。クラウド上の検査は実機確認を兼ねません。

- 最後の除外後に武器と補助を1回タップして取得できること
- 武器面1→3→2、補助面3→1→2でも戦場の位置・接続線・装置一覧が一致すること
- 回転冠の盾と入射面が一致して見えること
- 演出最小、音0でも近い敵・予告・敵弾を追えること
- 回転、中断復帰、再戦後も操作でき、既存の進行が残ること

未実測のフレーム速度、発熱、人間の勝率は合格にしません。

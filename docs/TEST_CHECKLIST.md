# カコマレ 検査チェックリスト

> 拡張の現行計画は[v2.0](EXPANSION_IMPLEMENTATION_PLAN.md)、進行・検査結果・未達の正本は[EXPANSION_PROGRESS.md](EXPANSION_PROGRESS.md)です。本書の過去工程の状態は当時の履歴です。

## V3実装：12武器・8補助・発展・配置変更（2026-09-11）

対象branchは `codex/v3-weapons-synergy-20260911`。基準mainは、マージ済みPR #20の後の `353295475354c145b06cb836543f9c5fd20b6141`。V3は、最終50武器・補助S=20へ向けた最初の実装地点として、既存8武器のLv1〜8・分岐・Lv8発展、追加4武器（迎撃格子・軌道機雷・蓄圧槍・追尾子機）、追加2補助（継電環・整備環）、敵状態の非色表現、停止中の移設・同種入替を実装する。残る38武器・12補助は設計済み・未実装のまま維持し、12種類を最終完成とは数えない。

- [x] `npm ci --ignore-scripts --no-audit --no-fund`
- [x] `npm run lint`
- [x] `npm run typecheck`
- [x] `npm run test -- --testTimeout=30000`（24ファイル、178件）
- [x] `npm run verify:expansion-docs`（生成文書5ファイル）
- [ ] `npm run test:e2e:chromium`（32件すべて起動前に失敗。`chromium_headless_shell-1234` の実行ファイルがローカルにないため。Draft PRのCIで確認）
- [ ] `npm run test:e2e:webkit`（32件すべて起動前に失敗。`webkit-2336/pw_run.sh` がローカルにないため。Draft PRのCIで確認）
- [x] `npm run build`（Viteの500 kB超チャンク警告あり）
- [x] `npm run verify:dist`
- [x] `npm run verify:originality`
- [x] `git diff --check`

ビルド後に配布物検査を順番に実行し、`build`、`verify:dist`、`verify:originality`、`git diff --check` は成功した。Chromium/WebKitはテスト本体へ到達せず、ローカル環境の実行ファイル不足で未実施である。最初に並行実行した `verify:dist` はビルド完了前のため `dist/index.html` 不在となったが、ビルド完了後の再実行は成功している。

V3の追加検証は、`tests/integration/battle-quality.test.ts` の4武器の実戦オブジェクト生成、迎撃格子の方向上限と軌道機雷の一回爆発、停止中の移設・入替と入力台帳、`tests/unit/upgrade.test.ts` のLv8発展候補、`tests/unit/enemy-rendering.test.ts` の状態記号を含む。`tests/e2e/core.spec.ts` には、一時停止の「装置を確認」から実際の移設ボタンを使う画面検査を追加した。内部メソッドの直接呼び出しによる統合検査と、画面操作による検査は別に記録する。

V3で確認していないものは、25/50武器、補助20の残り、全50×20の実装適用、全武器の実戦相乗効果、敵・無限モードの再構成、通常720試行・無限60試行、30/60/120回描画比較、保存v3、ランキング受付・本番DB、iPhone Safari実機である。自動ブラウザ検査はGitHub Actionsの提出commitで確認後に追記し、iPhone実機・VoiceOver・片手操作・発熱は未確認のまま残す。

## V1設計：50武器・補助・競技契約（2026-09-11）

対象branchは `codex/v1-competitive-catalog-20260911`。V1では設計済みの基本武器50、補助20、適用1,000セル、相乗効果100件、競技ルール案を検証し、設計のみの内容をruntime registryへ追加しない。実装・長時間本戦・ランキング送信・本番DBは後続工程です。

- [x] `npm ci --ignore-scripts --no-audit --no-fund`（162パッケージ）
- [x] `npm run lint`
- [x] `npm run typecheck`
- [x] `npm run test`（23ファイル、165件）
- [x] `npm run verify:expansion-docs`（生成文書5ファイル）
- [x] `npm run build`（Viteの500 kB超警告あり）
- [x] `npm run verify:dist`
- [x] `npm run verify:originality`
- [x] `git diff --check`
- [ ] `npm run test:e2e:chromium`（ローカルにブラウザ実行ファイルがなく未実行。Draft PRのCIで確認）
- [ ] `npm run test:e2e:webkit`（ローカルにブラウザ実行ファイルがなく未実行。Draft PRのCIで確認）

新しい検証IDは、全50列挙・重複監査・50×20適用表・各武器2方向の相乗効果・設計のみのruntime非混入を `tests/unit/expansion-catalog.test.ts` で確認する。CIの結果、iPhone Safari実機、公開配備、本番ランキング受付は [EXPANSION_PROGRESS.md](EXPANSION_PROGRESS.md) に対象コミットとともに記録します。

## V2競技基盤：個体・接続・容量・乱数（2026-09-11）

対象branchは `codex/v2-competitive-foundation-20260911`。基準mainはPR #19のマージコミット `edb84e0800d9ed657206c246241f9fafe5fe0014`。V2は既存runtimeの8武器・6補助を対象に、6→12→18面のBuildGraph、稼働容量、設置原点と反射境界、競技初期条件のローカルフック、目的別乱数、実効値、検証用入力台帳を確認する。50武器・補助20・相乗効果・新敵は設計または後続工程であり、V2の実装済み数へ加えない。

- [x] `npm ci --ignore-scripts --no-audit --no-fund`（162パッケージ）
- [x] `npm run lint`
- [x] `npm run typecheck`
- [x] `npm run test -- --testTimeout=30000`（24ファイル、172件。V2追加7件）
- [ ] `npm run test:e2e:chromium`（31件すべて起動前に失敗。PlaywrightのChromium実行ファイル不在）
- [ ] `npm run test:e2e:webkit`（31件すべて起動前に失敗。PlaywrightのWebKit実行ファイル不在）
- [ ] iPhone Safari、VoiceOver、片手操作、発熱、長時間の実機検査
- [ ] 30/60/120回描画比較、18面最大負荷、通常720試行、競技無限60試行

| ID | 確認内容 | 実装・検査 | 状態 |
|---|---|---|---|
| B01/B02 | 層1の6面、層2の12面、層3の18面を順に開き、面を飛ばした装着を拒否 | `BuildGraph`単体、V2候補の層開放経路 | コード検査済み。画面操作未確認 |
| B03/B07 | 同じ武器・補助を個体IDとnodeIDで分離し、接続を層内で維持 | 個体型、グラフ占有、スナップショット単体 | コード検査済み。複数同種の実戦は後続 |
| B04 | 補助の接続線と実効値計算の対象を一致させる | `connections`、`adjacentWeaponSlots`、実効値単体 | コード検査済み |
| B05/B06 | 設置原点、層別戦場半径、反射境界、旧弾の生成時境界を保持 | `ArenaGeometry`単体、Projectileスナップショット | コード検査済み。層拡張の長時間比較は未実施 |
| C06/C07 | 競技フックで研究依存の基礎能力を外し、容量6/12/18と候補の合法性を扱う | `competitive`初期化、`BuildCapacity`、候補単体 | コード検査済み。ランキング受付未接続 |
| C08/C09 | 候補・敵出現・戦闘効果・表示の乱数を分離し、入力をtick/角度で正規化 | `RandomStreams`、`InputRecorder`単体 | コード検査済み。描画頻度比較は未実施 |
| C14 | 結果へ個体別記録・ルール版・入力台帳・構成スナップショットを渡す | `RunRecorder`、`BattleResult`型 | コード検査済み。途中保存復元はV6 |

V2で追加した構造は、設計済みの50武器・補助20を実行時registryへ登録しません。本番ランキング通信、Supabase/実験場、DB・権限・公開設定は変更していません。自動ブラウザ検査が起動できない環境の失敗を成功へ繰り上げず、GitHub Actionsの提出commit結果と実機結果を別記録にします。

## PR-Aの品質検査

対象ブランチ: `codex/quality-a-20260908`
実施日: 2026-09-08（UTC）
開始時のmain: `e6ca4229156819ccce8b47ef4273b2e0451d4d75`

現在の実行結果、対象変更、再現条件、独立レビューは [QUALITY_A_REPORT.md](QUALITY_A_REPORT.md) にまとめます。公開版と実機の確認は [RELEASE_CHECKLIST.md](RELEASE_CHECKLIST.md) に記録します。

以前のmainに対する成功は、今回の変更に対する成功へ繰り越しません。[監査基準の検査記録](https://github.com/chameleonjp-lab/kakomare/blob/e6ca4229156819ccce8b47ef4273b2e0451d4d75/docs/TEST_CHECKLIST.md) は履歴として参照できます。

## コマンド

- [x] `npm ci --ignore-scripts --no-audit --no-fund`（162パッケージ）
- [x] `npm run lint`
- [x] `npm run typecheck`
- [x] `npm run test`（20ファイル、135件）
- [ ] `npm run test:e2e:chromium`
- [ ] `npm run test:e2e`相当（ActionsでChromium、WebKitを順番に実施）
- [x] `npm run build`（Viteの500 kB超警告あり）
- [x] `npm run verify:dist`
- [x] `npm run verify:originality`
- [ ] `npm run check`相当（Actionsで各検査を順番に実施）

## 画面

- [ ] 初回起動で名前入力が表示される
- [ ] 空文字、制御文字、13文字以上が拒否される
- [ ] 名前確定後にホームが表示される
- [ ] ステージ1を選べる
- [ ] 3、2、1のカウントダウン中に戦闘が進まない
- [ ] 開始直後に一時停止にならない
- [ ] 強化候補を1回タップで選べる
- [ ] 一時停止、再開、リタイアが動く
- [ ] 縦画面の戦闘中は装置一覧を一時停止内の「装置を確認」から開ける
- [ ] 勝利と敗北の両方が結果画面へ進む
- [ ] 結果画面は最上部から表示される
- [ ] ステージ1、2、3を順に勝利して無限モードを解放できる

## 表示幅

- [ ] 320 × 568
- [ ] 375 × 667
- [ ] 390 × 844
- [ ] 402 × 874
- [ ] 430 × 932
- [ ] 844 × 390
- [ ] 932 × 430
- [ ] 1280 × 720
- [ ] 1920 × 1080

既存のブラウザ検査では戦闘画面の寸法と配置を確認します。強化候補内の全操作の48px計測、文字200%、iPhone実機の見やすさはPR-B以降の確認です。

## ライフサイクルと負荷

- [ ] `visualViewport`の高さ変更で戦闘画面が収まる
- [ ] 画面回転時に戦闘が停止し、再開できる
- [ ] 非表示から復帰しても黒画面にならない
- [ ] 長押し、二重タップ、指を戦場外へ出して離す操作が破綻しない
- [ ] 演出量の自動調整が標準、少ない、最小の順で装飾を減らし、敵・敵弾の表示数を減らさない
- [ ] 危険予告と敵弾が通常の演出より前面に表示される
  - キャンバス内の深度は `src/game/render/RenderLayer.ts` で固定済み。iPhone実機での目視確認は公開前に実施する。
- [ ] 敵、弾、粒子、演出の上限を超えて増え続けない

## ロジック回帰

- [ ] 群集弾が指定位置まで飛び、着弾前の0.45秒だけ予告してから範囲攻撃する
- [ ] 敵ごとの衝突半径を使い、敵の見た目サイズに応じて判定する
- [ ] 手動照準の表示扇形と対象選択を同じ60度で扱う
- [ ] 周回刃の回転速度が発射間隔短縮の影響を受けない
- [ ] 候補が3枚揃わない強化抽選で経験値を消費しない
- [ ] 補助効果を文書化した上限で止める
- [ ] 背景、装置、味方演出、敵、危険予告、敵弾の描画深度を後ろから前へ固定する
- [ ] 攻撃成功時の被害数字を表示し、演出量と動きを減らす設定の上限を守る
- [ ] 通常の小型敵は満タン時の体力バーを省略し、特殊敵・ボス・被害敵は表示する
- [ ] 方向集中波は1.2秒以上前に外周の該当方向を予告し、予告中の3枠を予約する

## 共有と公開

- [ ] ホーム共有と結果共有で文章が異なる
- [ ] ネイティブ共有またはクリップボードが使えない場合に画面内コピー欄が出る
- [ ] 破損保存から初期画面へ復帰し、退避データを確認できる
- [ ] favicon、OGP、canonical、GitHub Pagesサブパスが `npm run verify:dist` で確認できる
- [ ] GitHub Actionsの最新変更に対するChromiumとWebKitが成功する（実行URLは検証記録に記載）

## PR-Aで追加する回帰条件

- [ ] 最後の除外後も、武器・補助の空き面を選べる
- [ ] 除外と引き直しの残数が0でも、取得できる候補を無効にしない
- [x] 非連続の順で装置を取得しても、面と隣接効果と表示を一致させる（単体・結合検査）
- [x] 正負の角度境界、盾3枚の位相、入射面の一致（単体・結合検査）
- [x] 安全半径内外の吸引で、移動方向と距離を保つ（単体検査）
- [x] 座標軸上の連鎖終端破裂と、重力崩壊の攻撃倍率（結合検査）
- [x] 敵180体・敵弾80発をすべての演出設定で表示する（結合検査）
- [x] 本番の戦闘処理による再現検査と基準値を保存する

## PR-Bの追加確認

対象ブランチ: `codex/quality-b-20260908`
開始時のmain: `70f1e4ec397cb6d98c7bcb778f416aff641849c0`（PR-Aマージ後）

### 実効値と画面

- [x] 8武器×6補助の48組で、隣接判定とレベル3の実効値が一致する（単体検査）
- [x] 収束環は射程の上限（+35%）と弾速（+44%）を別々に適用する（単体検査）
- [x] 群集弾の分裂型が着弾後に3小弾を生成し、再分裂しない（実戦バンドル検査）
- [x] 反発輪の強反発型が押し戻し距離と威力を増やす（実戦バンドル検査）
- [x] 制動環が反発輪、群集弾の残留領域、遠隔重力点の減速へ適用される（実戦バンドル検査）
- [x] 通常ステージは残り時間、無限モードは経過時間をスナップショットへ保持する（実戦バンドル検査）
- [x] 強化候補を320×568、402×874、横画面、文字200%で表示し、横スクロールを発生させない（Chromium/WebKit）
- [x] 強化候補、新規装置の面ボタン、除外、引き直しをすべて48px以上で1回タップできる（Chromium/WebKit）
- [x] 結果画面に8武器の総与ダメージと補助装置の接続状況が役割付きで出る（Chromium/WebKit）
- [ ] 敵図鑑に敵・ボスの説明と解除状態が出る
- [ ] カウントダウン、着弾・被害・ボス・強化・勝敗・一時停止の音が状態別に鳴る（音量0では無音）

### PR-Bの自動検査

- [x] `npm run lint`
- [x] `npm run typecheck`
- [x] `npm test -- --testTimeout=30000`（21ファイル、145件）
- [x] `npm run build`
- [x] `npm run verify:dist`
- [x] `npm run verify:originality`
- [x] `git diff --check`
- [x] GitHub ActionsのChromiumとWebKit（Quality #32）

このPR-B記録にある「強化中は10%へ落とす」は、当時の履歴であり現行仕様ではない。PR-1で、強化中・配置確認中・構成変更中の戦闘時間、敵、弾、被害、攻撃間隔、出現処理、危険予告を完全停止へ変更した。変更理由と保存への影響は [EXPANSION_PROGRESS.md](EXPANSION_PROGRESS.md) に記録する。iPhone Safari実機、長時間の本戦、描画レート別一致はPR-1でも未確認事項であり、この工程の自動検査成功には含めない。

## PR-1：成長停止と選択画面の回帰検査（2026-09-09）

対象ブランチ: `codex/expansion-pr1-20260909`

実装では、候補表示と選択確定を分離し、現在の選択番号・候補・配置先を再検証してから経験値、レベル、装備、記録を一度だけ更新する。通常候補が不足しても継続強化で3枚を満たし、除外できる候補と取得できる候補を分離した。以下のユニット・統合検査は本番 `BattleScene` の処理または候補生成・成長処理を対象に実行した。

| ID | 確認内容 | 実装上の対応 | 結果 |
|---|---|---|---|
| X01 | 3武器Lv5・照準強化済み、補助Lv1/3/3、耐久90 | 残る補助を保持し、継続強化で3枚化 | 成功 |
| X02 | 合法な通常候補が2枚 | 通常2枚を残し、継続強化1枚を補完 | 成功 |
| X03 | 武器・補助が全上限 | 兵装研磨・追加外装・部品確保を提示 | 成功 |
| X04 | 通常候補をすべて除外 | 継続強化を除外不能な出口として提示 | 成功 |
| X05 | Lv27、経験値3085、必要量259 | 10回確定後にLv37、経験値90、次349 | 成功 |
| X06 | 同じ選択確定を2回送信 | 選択番号で二重確定を拒否 | 成功 |
| X07 | 古い候補・満杯または無効な配置先 | 最新候補と空き面を再検証し、状態を変更しない | 成功 |
| X08 | 同時撃破と致死被害 | 1更新分の終了判定を先に行い、強化を表示しない | 成功 |
| X09 | 強化画面で1分待機相当 | `step` が完全停止し、時間・被害・弾・予告が変化しない | 成功 |
| X10 | 強化中に非表示・回転・復帰 | 候補と経験値を保持し、照準入力を解除して再開 | ブラウザ未実行、統合ロジック成功 |

追加した確認は、3回選択後の保留・再開、同一候補での無意味な引き直し抑止、継続強化の結果精算を1回だけ行う処理、既存保存型を変更しないこと、UIの停止・再開経路である。X10のブラウザ操作、表示幅、実機Safariは環境制約により未確認として扱う。
